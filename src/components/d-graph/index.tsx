import { FunctionalComponent, h } from "preact";
import * as style from "./style.css";

const DGraph: FunctionalComponent = () => {
    return <h1 class={style.text}>DGraph</h1>;
};

export default DGraph;
